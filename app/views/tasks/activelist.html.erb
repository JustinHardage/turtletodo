<h1>Active Tasks</h1>

<p id="notice"><%= notice %></p>

<table>
  <tr>
    <th>Rank</th>
    <th>Name</th>
    <th>Create date</th>
    <th>Close date</th>
    <th>Current work log</th>
    <th>Total time</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.rank %></td>
    <td><%= task.name %></td>
    <td><%= task.create_date %></td>
    <td><%= task.close_date %></td>
    <td><a href="<%= edit_worklog_path(task.current_work_log) %>"><%= task.current_work_log %></a></td>
    <td><%= task.total_time %></td>
    <td><%= link_to 'Punch ' + task.get_punch_direction, punch_task_path(task) %></td> 
    <td><%= link_to 'Edit', edit_task_path(task) %></td>
    <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task', new_task_path %>
