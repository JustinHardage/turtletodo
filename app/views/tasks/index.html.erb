<h1>Listing tasks</h1>

<table>
  <tr>
    <th>Rank</th>
    <th>Name</th>
    <th>Createdate</th>
    <th>Closedate</th>
    <th>Currentworklog</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.rank %></td>
    <td><%= task.name %></td>
    <td><%= task.createDate %></td>
    <td><%= task.closeDate %></td>
    <td><%= task.currentWorkLog %></td>
    <td><%= link_to 'Show', task %></td>
    <td><%= link_to 'Edit', edit_task_path(task) %></td>
    <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Task', new_task_path %>
